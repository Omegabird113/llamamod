name: Build

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v6
    
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17.0.19'
        distribution: 'oracle'
        cache: gradle

    - name: Make gradlew executable
      run: chmod +x ./gradlew

    - name: Build
      run: ./gradlew build --no-daemon

    - name: Upload jar
      uses: actions/upload-artifact@v7
      with:
        name: LlamaBlocks-build
        path: build/libs/*.jar
